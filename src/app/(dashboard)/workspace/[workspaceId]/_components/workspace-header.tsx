type Props = {};

const WorkspaceHeader = (props: Props) => {
    return (
        <h2 className={"text-lg font-semibold"}>
            My Channel
        </h2>
    );
};
export default WorkspaceHeader;
